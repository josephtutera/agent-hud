# agent-hud

A macOS menu-bar readout for subscription usage, API-equivalent value, and the
health of the shared agent setup in `~/.agents`. Read-only: nothing here mutates
the machine.

Forked from [agent-dash](https://github.com/josephtutera/agent-dash), which
paired the same daemon with a terminal TUI.
